name: build_time_analyzer
description: A Flutter performance analysis plugin that identifies expensive widget builds, rebuild frequency, and frame performance during development.
version: 0.0.1
repository: https://github.com/example/build_time_analyzer
issue_tracker: https://github.com/example/build_time_analyzer/issues
homepage: https://github.com/example/build_time_analyzer

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:

